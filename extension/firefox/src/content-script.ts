/**
 * Content script (Firefox build).
 *
 * Intentionally inert for privacy: ezcap observes from outside the page
 * and never injects observation hooks into page content. See the
 * Chromium build's content-script.ts for the rationale.
 */

export {};  // no behavior by design
